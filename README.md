# 认知翻译 · 把难懂的话翻译成人话

> 把世界上难懂的话，翻译成每个人都能真正看懂的话——并告诉你下一步该做什么。

## 在线使用

打开：**https://z9cwwf48w4-lab.github.io/cognitive-translate/**

- **不配密钥**：基础模式用内置词库做「术语拆解」，并明确告知“哪些超出了词库、无法整句翻译”，不假装翻译（离线可用）。
- **配密钥（可选）**：点「AI 设置」，填入你自己的 DeepSeek API Key（[platform.deepseek.com](https://platform.deepseek.com) 创建），**任意句子**都能翻译成四段式：30 秒看懂总结 → 生活例子 → 为什么重要 → 下一步；还支持“换个说法”和 AI 识别卡点词。

> 隐私：选中的文字只发送给你填写的模型服务商用于翻译；密钥只存在你自己的浏览器本地；本站没有服务器、不收集任何内容。

## v4 · 收敛（本轮评审后的更新）

核心流程收敛为：**原文 → 找出卡点 → 一句话说清真正意思 → 为什么 → 例子/下一步 → 没懂就换方式追问**。

- 结果页三级信息架构：「这句话真正想说的是」成为视觉中心；为什么/例子/下一步收敛为轻量三栏。
- 基础模式诚实降级：无 AI 时不假装整句翻译，只拆词并提示启用 AI。
- AI 增加 `caveat`（“这是简化理解，实际还受…影响”）与更保守的「下一步可以关注什么」（不再做投资建议）；校正了现金流/QE/参数等表述。
- 追问闭环：还是没懂 / 为什么 / 举个真实例子 → AI 真的换一种方式再解释。
- 反馈持久化：评分与追问记录存本机 localStorage（可导出分析）。
- 安全：支持「自有后端代理」模式（`server/server.mjs` 零依赖模板，密钥放服务端环境变量），直连模式明确标注为原型方案。
- 技术：`URLSearchParams` 解析、AI JSON schema 校验、术语高亮改为先分词再生成 HTML、aria-expanded/aria-pressed、桌面端三栏自适应。

## 浏览器插件（Chrome / Edge）

1. 下载本仓库 `extension/` 文件夹（或 [认知翻译-原型/插件] 中同款）。
2. 打开 `chrome://extensions`（Edge：`edge://extensions`）→ 开启右上角“开发者模式”。
3. “加载已解压的扩展程序” → 选择 `extension/`。
4. 在任意网页选中文字 → 右键 → 「认知翻译：把这段话翻译成人话」。

（上架 Chrome 应用商店的流程已备好，按需发布即可让普通用户一键安装。）

## macOS 全局划词（阅读器 / PDF / AI 工具等任何软件）

按仓库内说明创建“快速操作”：选中文字 → 右键 → 服务/快速操作 → 认知翻译，即调用本公网页面翻译。也可绑定快捷键。

## 文件说明

- `index.html` — 单文件应用（词库 + AI 模式都在里面，可整站复制到任意静态托管）。
- `extension/` — Chromium 划词插件。

## 路线

- [x] 本地可交互原型（规则引擎）
- [x] AI 模式：自带密钥、任意句子四段式翻译（DeepSeek 直连，无需后端）
- [x] 公网托管（GitHub Pages）
- [x] 浏览器划词插件（开发者模式安装）
- [ ] 上架 Chrome / Edge 应用商店
- [ ] 多模型切换（OpenAI / Claude / Gemini 等 OpenAI 兼容接口均可填）
- [ ] 历史记录与“成长感”（第二次看同一概念秒懂）
